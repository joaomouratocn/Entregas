<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Entregas" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/primary</item>
    </style>

    <style name="buttonCustom" parent="@style/Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="bg_header">
        <item name="android:background">@color/primary_dark</item>
    </style>

    <style name="textCustom" parent="@style/TextAppearance.AppCompat.Title">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>